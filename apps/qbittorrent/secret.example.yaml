# TEMPLATE ONLY — the real secret ships SOPS-encrypted in this directory
# (*.sops.yaml); this file just documents its shape. Not referenced by kustomization.
---
apiVersion: v1
kind: Secret
metadata:
  name: qbittorrent-vpn
  namespace: qbittorrent
type: Opaque
stringData:
  # ProtonVPN WireGuard private key from the Proton portal.
  WIREGUARD_PRIVATE_KEY: "REPLACE_ME"
  # WireGuard interface address, e.g. 10.2.0.2/32.
  WIREGUARD_ADDRESSES: "10.2.0.2/32"
