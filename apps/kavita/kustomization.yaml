---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - pvc.yaml
  - persistentvolume.yaml
  - pvc-library.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - bootstrap
  - networkpolicies

labels:
  - pairs:
      app.kubernetes.io/part-of: kavita
