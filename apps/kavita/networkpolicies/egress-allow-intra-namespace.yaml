---
# With cluster-wide default-deny egress in effect, same-namespace traffic must be
# allowed on the egress side too.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-allow-intra-namespace
  namespace: kavita
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kavita
