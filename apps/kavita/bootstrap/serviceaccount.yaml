---
# Identity for the daemon; needs a token (see deployment) to trigger a single
# rolling restart of Kavita when the OIDC connection settings change.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kavita-bootstrap
  namespace: kavita
