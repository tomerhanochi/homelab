---
# Identity for the bootstrap Job; needs a token (see job.yaml) to trigger a
# single rolling restart of Kavita when the OIDC connection settings change.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kavita-bootstrap
  namespace: kavita
