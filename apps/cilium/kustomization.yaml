---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Generic cluster-wide Cilium policies (default-deny + shared allows). App-specific
# cluster-wide policies live with their app.
resources:
  - ciliumclusterwidenetworkpolicy

labels:
  - pairs:
      app.kubernetes.io/part-of: cilium
