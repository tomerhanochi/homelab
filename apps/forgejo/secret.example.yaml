# TEMPLATE ONLY — the real secret ships SOPS-encrypted in this directory
# (*.sops.yaml); this file just documents its shape. Not referenced by kustomization.
---
# CloudNativePG credentials for the forgejo-database cluster owner.
apiVersion: v1
kind: Secret
metadata:
  name: forgejo-database-credentials
  namespace: forgejo
type: kubernetes.io/basic-auth
stringData:
  username: forgejo
  password: REPLACE_WITH_RANDOM_PASSWORD
---
# OIDC client credentials for the authentik "forgejo" provider.
apiVersion: v1
kind: Secret
metadata:
  name: forgejo-oauth
  namespace: forgejo
type: Opaque
stringData:
  key: forgejo
  secret: REPLACE_WITH_OIDC_CLIENT_SECRET
