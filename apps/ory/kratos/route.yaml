---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: identity
  namespace: ory
spec:
  parentRefs:
    - name: default
      namespace: gateway
      sectionName: identity
  hostnames:
    - identity.tomerhanochi.com
  rules:
    - backendRefs:
        - name: kratos-public
          port: http
