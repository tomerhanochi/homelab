---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: auth
  namespace: ory
spec:
  parentRefs:
    - name: default
      namespace: gateway
      sectionName: auth
  hostnames:
    - auth.tomerhanochi.com
  rules:
    - backendRefs:
        - name: hydra-public
          port: 4444
