---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: accounts
  namespace: ory
spec:
  parentRefs:
    - name: cilium-gateway
      namespace: cilium
      sectionName: accounts
  hostnames:
    - accounts.tomerhanochi.com
  rules:
    - backendRefs:
        - name: kratos-selfservice-ui-node
          port: 80
